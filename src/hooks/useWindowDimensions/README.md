# Use this hook like

```const Component = () => {
  const { height, width } = useWindowDimensions(); // This is the important line

  return (
    <div>
      width: {width} ~ height: {height}
    </div>
  );
}
```
